[build]
builder = "nixpacks"
buildCommand = "pip install -r telegram-bot/requirements.txt"

[deploy]
startCommand = "cd telegram-bot && python bot.py"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
